

#include "AudioListener.hpp"

namespace HexEngine
{
	AudioListener::AudioListener(Entity* entity) : 
		BaseComponent(entity)
	{

	}

	AudioListener::AudioListener(Entity* entity, AudioListener* copy) : 
		BaseComponent(entity)
	{

	}
}